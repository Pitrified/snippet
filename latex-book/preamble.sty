\ProvidesPackage{example}

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
% \usepackage[italian]{babel}
\usepackage[english]{babel}
\usepackage{marginnote}

%%%%% Bibliography %%%%%
\usepackage[
    backend=biber,
    style=alphabetic,
    sorting=ynt,
]{biblatex}
% \usepackage[nottoc]{tocbibind}	% to generate Bibliography entry in toc
% mah https://tex.stackexchange.com/a/71153
% sorting order https://tex.stackexchange.com/a/51439
% year name title

\usepackage{amsmath,amssymb,amsfonts,amsthm}
\usepackage{bm}
\usepackage[top=3cm, bottom=3cm, heightrounded, marginparwidth=2.5cm, marginparsep=1.5cm]{geometry}
\usepackage{graphicx}
\graphicspath{ {images/} }
% per poter usare subfigure devi importare questo package
\usepackage{subcaption}

\usepackage{lipsum}

\usepackage{enumitem} % more control over lists: stackoverflow.com/a/4974583
\usepackage{indentfirst} % YES indent after section/chapter heading

\usepackage[Algoritmo]{algorithm}
\usepackage[noend]{algpseudocode} % algoritmi decenti, senza end

\usepackage{tikz} % for tikzpicture
\usetikzlibrary{shapes} % node shapes
% fancy plots overleaf.com/learn/latex/Pgfplots_package
\usepackage{pgfplots}
\pgfplotsset{width=6cm,compat=1.9}
% \usetikzlibrary{external} % save plots in pdf for faster compiling
% \tikzexternalize[prefix=tikz/] % save them here

% people say this should be the last package added...
\usepackage{hyperref}	% enable hyperlinks to referenced elements
\hypersetup{
	colorlinks=true,
	linkcolor=cyan,
	citecolor=cyan,
    linkcolor=cyan,
    filecolor=magenta,
    urlcolor=blue,
}
\urlstyle{same}

%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%   CUSTOM THEOREM/DEF  %%

\theoremstyle{definition}
\newtheorem{definition}{Definizione}[section]

\theoremstyle{theorem}
\newtheorem{theorem}{Teorema}[section]

%%   END THEOREM         %%
%%%%%%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%   CUSTOM COMMANDS     %%

% \newcommand{\rfig}[1]{Figure~\ref{fig:#1}} % thanks to Seba for the keystrokes saved
\newcommand{\rfig}[1]{Figure~\ref{#1}} % thanks to Seba for the keystrokes saved

%%   END COMMANDS        %%
%%%%%%%%%%%%%%%%%%%%%%%%%%%

% magic headers
\usepackage{fancyhdr}
\makeatletter 
\pagestyle{fancy}
% using even and odd with oneside is kinda useless
% \fancyhead{}
% \fancyhead[LE,RO]{\@title}
% \fancyfoot{}
% \fancyfoot[LE,RO]{\thepage}
% \fancyfoot[LO,RE]{Chapter \thechapter}
% \fancyfoot[CO,CE]{\@author}
\lhead{\@title}
\chead{}
\rhead{}
\lfoot{Chapter \thechapter}
\cfoot{\@author}
\rfoot{\thepage}
\makeatother 





